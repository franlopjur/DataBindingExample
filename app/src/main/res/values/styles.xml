<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AndroidVersionTitle">
        <item name="android:layout_marginEnd">@dimen/margin__normal</item>
        <item name="android:layout_marginLeft">@dimen/margin__normal</item>
        <item name="android:layout_marginRight">@dimen/margin__normal</item>
        <item name="android:layout_marginStart">@dimen/margin__normal</item>
        <item name="android:layout_marginTop">@dimen/margin__big</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="EmptyViewTitle">
        <item name="android:layout_margin">@dimen/margin__normal</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
</resources>
